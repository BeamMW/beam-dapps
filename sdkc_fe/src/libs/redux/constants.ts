export enum ACTIONS {
  LOADING = 'LOADING',
  ERROR = 'ERROR',
  SET_ITEMS = 'SET_ITEMS',
  SET_PIC = 'SET_PIC',
  SET_PKEY = 'SET_PKEY',
  SET_CID = 'SET_CID',
  SET_TX = 'SET_TX'
}

export enum STATUS {
  IN_PROGRESS = 'in progress',
  FAILED = 'failed',
  COMPLETED = 'completed'
}
